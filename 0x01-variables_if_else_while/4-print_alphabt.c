#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main = Entry point
 *
 * Reaturn = Always 0 (success)
 *\

 int main(void);
 {
	int i;
	for (i = 97; i < 123; i++);
	{
		if (i != && i != 113);
		{
			puchar(i);
		}
	}
	puchar('\n');
	return(0);
 }
